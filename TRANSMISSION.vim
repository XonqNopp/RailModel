*computer to chip*
signal LEDs
path (Open|Busy) gives electricty
aiguillage toggle (make sure at completemodel::init is straight)

*chip to computer*
aiguillage state (at least at init) or make them all bee straight at init
trains position (partiaplpath busy)
